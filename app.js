const form=document.getElementById("form"),input=document.getElementById("input"),chat=document.getElementById("chat"),send=document.getElementById("send");
function add(text,who){const d=document.createElement("div");d.className="msg "+who;if(who==="ai"){const b=document.createElement("b");b.textContent="Hessin AI";d.appendChild(b)}const s=document.createElement("div");s.textContent=text;d.appendChild(s);chat.appendChild(d);chat.scrollTop=chat.scrollHeight;return s}
form.addEventListener("submit",async e=>{e.preventDefault();const text=input.value.trim();if(!text)return;add(text,"user");input.value="";send.disabled=true;send.textContent="…";const thinking=add("أفكر في المهمة…","ai");try{const r=await fetch("/api/chat",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:text})});const data=await r.json();thinking.textContent=data.text||data.error||"حدث خطأ."}catch{thinking.textContent="تعذر الاتصال بالخادم."}finally{send.disabled=false;send.textContent="إرسال";input.focus()}});
input.addEventListener("keydown",e=>{if(e.key==="Enter"&&!e.shiftKey){e.preventDefault();form.requestSubmit()}});
